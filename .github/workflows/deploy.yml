name: Deploy React App to GitHub Pages

on:
  push:
    branches:
      - master

permissions:
  contents: write
  pages: write # Required for deploying to GitHub Pages

jobs:
  build:
    runs-on: ubuntu-latest
    environment: github-actions

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm install

      - name: Debug - Check env variables
        env:
          VITE_EMAIL_ADDRESS: ${{ secrets.VITE_EMAIL_ADDRESS }}
          VITE_PHONE_DIAL: ${{ secrets.VITE_PHONE_DIAL }}
        run: |
          echo "VITE_EMAIL_ADDRESS is set: ${VITE_EMAIL_ADDRESS:+yes}"
          echo "VITE_PHONE_DIAL is set: ${VITE_PHONE_DIAL:+yes}"
          echo "VITE_EMAIL_ADDRESS length: ${#VITE_EMAIL_ADDRESS}"
          echo "VITE_PHONE_DIAL length: ${#VITE_PHONE_DIAL}"

      - name: Build app
        env:
          VITE_EMAIL_ADDRESS: ${{ secrets.VITE_EMAIL_ADDRESS }}
          VITE_PHONE_DIAL: ${{ secrets.VITE_PHONE_DIAL }}
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./dist # Path to the build directory

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: github-pages
    permissions:
      pages: write # Required for deploying to GitHub Pages
      id-token: write # Required for authentication

    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v5
